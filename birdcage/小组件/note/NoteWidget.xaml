<UserControl x:Class="DynamicBird.UI.Widgets.Notes.NoteWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="clr-namespace:DynamicBird.UI.Localization">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 便签标签栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" 
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="NoteTabs" Background="Transparent">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="TabBorder"
                                    Background="{Binding ColorBrush}"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    Margin="0,0,4,0"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="Tab_Click">
                                <TextBlock Text="{Binding Title}" 
                                           FontSize="11" 
                                           Foreground="#1E1E1E"
                                           MaxWidth="120"
                                           TextTrimming="CharacterEllipsis"/>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                    <Setter TargetName="TabBorder" Property="BorderBrush" Value="#0078D4"/>
                                    <Setter TargetName="TabBorder" Property="BorderThickness" Value="2"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                <Button x:Name="BtnToggleTitle" 
                        Content="📝" 
                        Width="24" Height="24"
                        FontSize="14"
                        Background="Transparent"
                        Foreground="#888888"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="{Binding Item[UI_NoteWidget_407], Source={x:Static loc:LocalizationManager.Instance}}"
                        Click="ToggleTitle_Click"/>
                <Button x:Name="BtnColorPicker" 
                        Content="🎨" 
                        Width="24" Height="24"
                        FontSize="14"
                        Background="Transparent"
                        Foreground="#888888"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="{Binding Item[UI_NoteWidget_408], Source={x:Static loc:LocalizationManager.Instance}}"
                        Click="ColorPicker_Click"/>
            </StackPanel>
        </Grid>

        <!-- 便签内容（滚动，限高 300：内容长时面板不无限增高） -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="300">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" 
                         x:Name="TitleEditor"
                         Text="{Binding CurrentNote.Title, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="13" 
                         FontWeight="SemiBold"
                         Foreground="#EEEEEE"
                         Background="Transparent"
                         BorderThickness="0"
                         Visibility="{Binding ShowTitleVisibility}"
                         TextChanged="Title_TextChanged"/>

                <TextBox Grid.Row="1" 
                         x:Name="ContentEditor" 
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="Wrap"
                         FontSize="13"
                         Foreground="#EEEEEE"
                         Background="Transparent"
                         BorderThickness="0"
                         MinHeight="120"
                         TextChanged="Content_TextChanged"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>