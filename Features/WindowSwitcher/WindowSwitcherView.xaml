<UserControl x:Class="LingDongBird.Features.WindowSwitcher.WindowSwitcherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="RootControl">
    <ScrollViewer x:Name="MainScrollViewer" 
                  VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Auto"
                  MaxHeight="250">
        <ItemsControl x:Name="WindowList" 
                      Background="Transparent"
                      ItemsSource="{Binding Windows}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel x:Name="ItemsWrapPanel" 
                               Orientation="Vertical"
                               ItemWidth="{Binding ElementName=RootControl, Path=TagWidth, FallbackValue=120}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="Transparent" 
                            Padding="8,4" 
                            Cursor="Hand"
                            MouseLeftButtonUp="OnItemClick"
                            MouseDown="Border_MouseDown"
                            MouseMove="Border_MouseMove"
                            MouseUp="Border_MouseUp">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Image Grid.Column="0" 
                                   Source="{Binding Icon}" 
                                   Width="16" Height="16" 
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>

                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Title}" 
                                       FontSize="13" 
                                       Foreground="#EEEEEE"
                                       TextTrimming="CharacterEllipsis"
                                       MaxWidth="200"
                                       VerticalAlignment="Center"/>

                            <Button Grid.Column="2" 
                                    Content="✕" 
                                    Width="20" Height="20"
                                    FontSize="12"
                                    Foreground="#AAAAAA"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Click="OnCloseClick"
                                    ToolTip="关闭窗口"
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Center">
                                <Button.Resources>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="Background" Value="#CC3333"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>