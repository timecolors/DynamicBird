<Window x:Class="LingDongBird.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="灵动鸟"
        Height="300"
        Width="380"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">

    <Grid>
        <Border x:Name="MainPanel"
                Background="#2D2D2D"
                CornerRadius="16"
                Padding="15,10"
                Opacity="0"
                MouseEnter="MainPanel_MouseEnter"
                MouseLeave="MainPanel_MouseLeave"
                MouseDown="MainPanel_MouseDown"
                MouseMove="MainPanel_MouseMove"
                MouseUp="MainPanel_MouseUp">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 系统状态栏 -->
                <ContentControl x:Name="SystemStatusContainer" 
                                Grid.Row="0"
                                Height="28"
                                Margin="0,0,0,6"/>

                <!-- 主内容区 -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               x:Name="IconText"
                               Text="🐦"
                               FontSize="22"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"
                               Cursor="Hand"/>

                    <ContentControl x:Name="ContentContainer"
                                    Grid.Column="1"
                                    VerticalAlignment="Center"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>